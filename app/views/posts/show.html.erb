<div class='container'>

<br/>




  <div class="jumbotron">
    <h1 class="display-4">  <%= @post.title %></h1>

    <hr class="my-4 overflow-auto">
    <p>  <%= simple_format( @post.body )%></p>
    <div class="lead">

  <%= link_to "Edit the recipe", edit_post_path(@post), :class => 'btn btn-success' %>
  <%= link_to "Delete the recipe", post_path(@post),
                                 method: :delete,
                                 data: {confirm: 'Do you really want to do it?'},
                                :class => 'btn btn-danger' %>

    </div>
  </div>



<%= render 'comments/commentsPv' %>

<%= render 'comments/commentsformPv' %>



</div>
